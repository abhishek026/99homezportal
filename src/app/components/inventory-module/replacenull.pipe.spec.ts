import { ReplacenullPipe } from './replacenull.pipe';

describe('ReplacenullPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplacenullPipe();
    expect(pipe).toBeTruthy();
  });
});
